<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.leverett.chessrepertoirepractice">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChessRepertoirePractice">
        <activity android:name=".TestActivity"></activity>
        <activity android:name=".SandboxActivity" />
        <activity
            android:name=".ui.activities.SandboxActivity"
            android:label="@string/title_activity_sandbox"
            android:theme="@style/Theme.ChessRepertoirePractice.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>